{
  "name": "@crypticdot/defituna-rust-core",
  "description": "DefiTuna core rust library.",
  "version": "3.6.19",
  "private": true,
  "files": [],
  "scripts": {
    "build": "cargo build -p defituna-core",
    "test": "cargo test -p defituna-core --lib",
    "deploy": "../../scripts/deploy-cargo",
    "format": "cargo clippy --fix --allow-dirty --allow-staged && cargo fmt",
    "lint": "cargo clippy && cargo fmt --check",
    "clean": "cargo clean -p defituna-core"
  }
}